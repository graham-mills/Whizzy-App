@{
    string currentRoute = ViewContext.RouteData.Values["controller"].ToString();
}
<header>
    <div class="container">
        <div class="nav navbar-default">
            <div class="navbar-header">
                <a href="@Url.Action("Index", "Home")"><h1>Whizzy<small>.js</small></h1></a>
            </div>
            <ul class="nav nav-pills navbar-right">
                <li @if (currentRoute == "About") { <text> class="active" </text>  }>@Html.ActionLink("About", "../About/")</li>
                <li @if (currentRoute == "Create") { <text> class="active" </text>  }>@Html.ActionLink("Create", "../Create/")</li>
                <li @if (currentRoute == "Doc") { <text> class="active" </text> }>@Html.ActionLink("Documentation", "../Doc/")</li>
            </ul>
        </div>
    </div>
</header>

